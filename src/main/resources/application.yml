server:
  port: 8000

spring:
  application:
    name: oauth2-server

logging:
  level:
    ROOT: INFO
    http.wire-log: DEBUG

logbook:
  format.style: http
  write:
    level: DEBUG
    category: http.wire-log

---
spring:
   profiles: test
logging:
  level:
    es.virtualdesk.rulest.domain.executor.ExecutorService.ExecutorRREL: TRACE
